# rpg_game.spec - PyInstaller configuration file

a = Analysis(
    ['step3_gui.py'],  # MAIN FILE - this is your entry point
    pathex=[],
    binaries=[],
    datas=[
        # Include all trained models
        ('npc_aggressive.zip', '.'),
        ('npc_defensive.zip', '.'),
        ('npc_strategist.zip', '.'),
        ('npc_chaotic.zip', '.'),
    ],
    hiddenimports=[
        'stable_baselines3',
        'gymnasium',
        'torch',
        'numpy',
        'tkinter',
    ],
    hookspath=[],
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)

pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    name='AI_RPG_Arena',  # Your EXE name
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # FALSE = No console window (game only)
    icon='icon.ico',  # Optional: add your own icon
)